// Refactor to use template strings
// function doubleMessage(number) {
//   return "Your number doubled is " + (2 * number);
// }

// TEMPLATE STRING
function doubleMessage(number) {
  return `Your number doubled is ${2 * number}`;
}

// EXERCISE 2
function fullName(firstName, lastName) {
  return `${firstName} ${lastName}`;
}
